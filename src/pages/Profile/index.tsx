import { IonContent} from "@ionic/react";
import { UserProfile } from "../../components/Profile";

export const Profile = () => {
  return (
    <IonContent>
      <UserProfile />
    </IonContent>
  );
};
